# Generated by Django 4.2.27 on 2026-01-02 00:38

from django.db import migrations


def create_preferences_for_existing_users(apps, schema_editor):
    """Create UserPreferences for all existing users who don't have one."""
    User = apps.get_model('predictions', 'User')
    UserPreferences = apps.get_model('predictions', 'UserPreferences')

    for user in User.objects.all():
        UserPreferences.objects.get_or_create(user=user)


def reverse_preferences(apps, schema_editor):
    """Remove all UserPreferences (for rollback)."""
    UserPreferences = apps.get_model('predictions', 'UserPreferences')
    UserPreferences.objects.all().delete()


class Migration(migrations.Migration):

    dependencies = [
        ('predictions', '0012_userpreferences'),
    ]

    operations = [
        migrations.RunPython(create_preferences_for_existing_users, reverse_preferences),
    ]
